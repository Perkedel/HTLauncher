package com.perkedel.htlauncher.enumerations

enum class ItemExtraPaneNavigate(val naming:String = "") {
    Default(naming = ""),
    SelectApp(naming = "SelectApp"),
    ReorderItem(naming = "ReorderItem")
}